# RAGamer

面向游戏攻略与资料的中文 RAG 助手。用户按游戏建库、用口语提问，系统先确认问的是哪款游戏、哪个版本，再检索并生成带引用的回答。目标是服务任何一款单机或网游，而不只是预置的那几款。

## Language

### 知识库

**游戏知识库**：
某个具体游戏的、与其他游戏互相隔离的检索单元，用户可在界面里自行创建。
_Avoid_: 数据集, dataset, 分区, 知识库分区

**预设游戏库**：
项目方预置了术语映射与主体类目的游戏知识库，全功能可用。
_Avoid_: 官方库, 内置库

**自定义游戏库**：
用户自行创建、未配置术语映射的游戏知识库。其主体类型与内容性质由模型自动归一，精度低于预设库——这是被明确接受的降级。
_Avoid_: 用户库, 第三方库

### 内容与标签

**切片**：
检索与生成的最小内容单元。
_Avoid_: 块, 片段, segment

**主体**：
一条内容所描述的游戏内对象。
_Avoid_: 实体, entity, 对象

**主体名**：
主体在游戏中的具体名称，如「二郎神」「寒江雪」。**文档级**——一份文档归属一个主体。
_Avoid_: 产品名, item_name, 实体名

**主体类型**：
主体所属的类别，共七类：角色／物品／地点／任务／技能／背景设定／机制系统。**文档级**。
_Avoid_: 实体类型, 内容分类, 标签

**内容性质**：
一个切片回答的是哪一类问题，共五类：介绍、位置与获取、数值、打法流程、评价推荐。**切片级**——同一文档内不同小节的性质不同，不可文档级统一。
_Avoid_: 内容类型, 问题类型, 意图

**游戏术语**：
某个游戏对一类对象的本地叫法，如黑神话的「妖王」「根器」、燕云的「心法」「门派」。
_Avoid_: 二级标签, 自定义标签, 别名

**术语映射**：
游戏术语到主体类型的对应关系，按游戏配置。
_Avoid_: 同义词表, 标签注册表

### 切分

**祖先标题路径**：
一个切片在源文档标题目录中的完整位置，形如「二郎神 › 打法 › 第二阶段」。区别于只记录一层的父标题。
_Avoid_: 父标题, 面包屑

**父子块**：
检索命中的是细粒度子块，交给生成的是其所属的聚合父块。
_Avoid_: 双层切分, 小块检索大块返回

**结构化文档 / 扁平文档**：
前者有可用的标题层级，按标题切；后者没有，按语义边界切。同一条 md 内部可以两者并存。
_Avoid_: 好文档／坏文档, 有格式／无格式

### 检索

**混合检索**：
在**同一批数据**上融合稠密与稀疏两路向量。这是**一路**召回内部的机制。
_Avoid_: 多路召回（两者不是一回事）

**多路召回**：
同一问题经**多条彼此独立**的检索策略取回候选，再融合。
_Avoid_: 多路检索

**查询路由**：
按问题类型决定本次走哪几路召回，不做无差别全跑。
_Avoid_: 意图分类（更窄，只判类型不决定路径）, 查询分类

**澄清反问**：
系统对确定不了的维度（哪款游戏、哪个版本）主动向用户提问。**候选必须来自语料中真实存在的选项，绝不由模型生成。**
_Avoid_: 消歧, 实体链接, 反问

**断崖截断**：
按候选之间的分数落差决定截断位置，而非固定取前 K 条。
_Avoid_: 动态 TopK, 自适应 TopK

### 对话

**会话**：
一次连续的多轮问答。绑定一个游戏知识库，问答记录落在服务端而不是页面里——刷新之后还要读得回来。列表里显示的是**标题**（取首轮问句截断，只在第一轮定下），按**最后活跃**倒序（每次落库刷新）。
_Avoid_: 聊天记录, session

**轮**：
会话里的**一问一答**：用户说的话与模型给的那个答案，各占一条消息。整轮一起落库（生成中途断掉就整轮不写），带进提问理解的历史窗口也按轮算。
_Avoid_: 回合, 一组消息

### 版本

**现行版本**：
该游戏当前生效的版本。

**未标注版本**：
不随版本变化的内容（世界观、基础设定）。检索任何版本时都应一并纳入——漏掉这一条会让用户在切换版本后突然答不出设定类问题。
_Avoid_: 无版本, 通用内容

### 内容来源

**词条内容**：
描述游戏内对象的资料性内容（属性、背景、掉落）。时效性弱。
_Avoid_: 百科, wiki 内容

**攻略内容**：
玩家写的、解决具体问题的内容（打法、路线、流派）。时效性强。
_Avoid_: 教程, 指南
