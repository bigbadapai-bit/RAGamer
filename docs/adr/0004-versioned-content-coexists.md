# 版本并存，不覆盖旧版本

游戏版本更新时，新版本的资料作为**新文档导入并与旧版本并存**，而不是替换旧文档。

直接后果是同一主体会在库里留下多个高度相似的版本副本：不做版本过滤时，近重复内容会同时被召回并稀释融合与精排的分数。因此**版本过滤必须默认开启**，且过滤条件必须是"等于所选版本**或**未标注版本"——漏掉后半句，用户在切到历史版本后就会突然答不出不随版本变化的世界观类问题，而这类静默失效极难定位。

**代价**：知识库只增不减，需要定期评估冷版本的清理策略。
